package com.ganesha.mip.repository;

import com.ganesha.mip.model.jpa.ProductBucketPerformanceDetailAgressiveEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductBucketPerformanceDetailAgressiveRepository extends JpaRepository<ProductBucketPerformanceDetailAgressiveEntity, String> {
    
}